# Main

- Criar o arquivo main
- instalar o express

- o main conhece todas as instancias do projeto

- criar testes?

- criar factory para atribuir instancias

- testes de integração ponta a ponta

sucrase -> compilacao de typescript para javascript

supertest @types/supertest

body parser
cors
default content type json

- importar arquivos aumaticamente ->fast-glob

docker run --name mongodb -d mongo
docker run --name mongodb -d -p 27017:27017 mongo

docker run --name mongodb -d -e MONGO_INITDB_ROOT_USERNAME=AzureDiamond -e MONGO_INITDB_ROOT_PASSWORD=hunter2 mongo

- conserta o npm start dando erro no mongo db e nos testes -> provavelmente ao consertar o npm start va consertar os teste